# CRITICAL AGENT INSTRUCTIONS - READ IMMEDIATELY

**IF YOU ARE AN AI AGENT READING THIS, STOP AND READ CAREFULLY. THE USER IS EXTREMELY STRICT ABOUT THESE RULES DUE TO PAST CATASTROPHIC DATA LOSS.**

## 1. NEVER OVERWRITE LEGACY FILES
Do NOT blindly sync or completely replace `styles.css`, `index.html`, `diagnostic.html`, or ANY other core file just to "match a new design" or "fix styling". If a page needs styling updates, you MUST carefully append or insert targeted CSS rules using exact surgical block replacements.

## 2. NEVER SILENTLY DELETE CONTENT
You are explicitly FORBIDDEN from removing existing content (like partner logos, navigation links, or existing feature cards) when asked to do unrelated tasks. Do NOT delete anything unless explicitly commanded by the user.

## 3. NO COLLATERAL CSS DAMAGE
Before applying global CSS overrides (especially `!important` tags or modifying `.container`/`.shell`), you MUST verify the exact downstream impact on mobile `@media` queries. Do NOT break mobile responsiveness or cause horizontal overflows.

## 4. EXPLICIT CONSENT REQUIRED
Before taking ANY action that drastically alters the legacy UI, drops entire directories/files, or significantly changes the CSS architecture, you MUST STOP and request explicit confirmation from the user. 

**FAILURE TO COMPLY WITH THESE RULES WILL RESULT IN SEVERE USER DISSATISFACTION.**
